# netlify.toml
[build]
  base = "fronten-car-rental"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--silent"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
